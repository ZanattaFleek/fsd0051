var presentes = ["Fred", "Wagner", "Felipe", "Rodrigo", "Rafael"];

var ultimoIndice = presentes.length - 1;

for (let index = 0; index <= ultimoIndice; index = index + 1) {
  console.log(presentes[index]);
}
