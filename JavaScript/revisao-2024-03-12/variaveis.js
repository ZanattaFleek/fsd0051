var salario = 1000; // Number

var empresa = "Fleek"; // String

var ativa = true; // Boolean

// var dataAtual = new Date(1974, 5, 24, 12, 0, 50); // Date
// var dataAtual = new Date(2007, 7, 22, 12, 0, 50); // Date

var dataAtual = new Date(1991, 4, 25, 12, 0, 50); // Date

// new Date(ano, mês, dia, hora, minuto, segundo, milissegundo);

// var salarioFormatado = (salario + 2000).toFixed(2);

// console.log(empresa.length);
console.log(dataAtual.toString());
